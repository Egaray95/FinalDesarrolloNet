@model IEnumerable<ClinicaP2.Models.Consulta>

@{
    ViewBag.Title = "ListConsultaMedico";
}

<h2>ListConsultaMedico</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ConsEstado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ConsFecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Paciente.PacCodigo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Trabajador.TraUsuario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ConsEstado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ConsFecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Paciente.PacCodigo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Trabajador.TraUsuario)
        </td>
        <td>
            @Html.ActionLink("Detalle", "DetailsConsulta", new { id=item.ConsCodigo }) |
         
        </td>
    </tr>
}

</table>
